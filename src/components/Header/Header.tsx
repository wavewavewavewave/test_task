import React from 'react';
import {PageHeader} from '@ant-design/pro-layout'

export const Header = () => {
    return (
        <PageHeader title={'Beer Table Demo'} subTitle={'with Ant Design'}/>
    );
};